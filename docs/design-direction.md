# Astera 홍보 웹사이트 — 첫 시안

> 이 문서는 초기 디자인 결정과 당시 검증 결과를 기록한다. 현재 웹사이트는 [astera.run](https://astera.run)에 배포되었으며, 최신 실행·언어·검색 설정은 [README](../README.md)를 참고한다.

2026-09-21. 루트: `D:\parsingk\astera-web`.

## 목적과 범위

Claude Code와 Codex를 사용하는 개발자가 Astera의 역할을 이해하고, 실제 화면과 데모를 확인한 뒤 운영체제에 맞는 설치본을 내려받는 소개 페이지와 기능·사용법을 설명하는 Guide 시안. 정적 HTML/CSS/JavaScript이며 배포나 운영 프레임워크 선택은 이번 범위에 포함하지 않는다.

## 근거

- 제품: `D:\parsingk\astera\README.md`, `README.ko.md`, `package.json`, `src/core/theme/themes.ts`.
- 이미지와 영상: Astera의 기존 `assets` 폴더. 로고: `resources/logo-source.png`. 원본을 그대로 복사했다.
- 참고: https://www.onorca.dev/ — 2026-09-21 확인. 큰 제품 미리보기, 기능 탐색, 다운로드 동선을 참고한다. Orca의 카피, 수치, 후기, 로고는 사용하지 않는다.

## 검토한 방향

1. **Daylight workspace (초기 시안)**: 밝은 바탕, 남색 타이포그래피, 큰 실제 제품 화면. 설명의 가독성이 높고 앱 화면이 선명하게 구분된다.
2. **Night observatory (현재 기본값)**: 짙은 남색 바탕과 별빛 포인트. 기존 로고와 밀접하며 앱 프레임의 경계와 빛으로 배경과 구분한다.
3. **Product manual**: 흑백과 기능 중심의 밀도 높은 구성. 정보 전달에 강하지만 첫 방문의 브랜드 인상은 약하다.

사용자가 처음에는 **밝은 배경 + 남색 제품 화면, 영어 중심**을 선택했고, 두 시안을 비교한 뒤 **다크 모드를 기본값**으로 확정했다. 헤드라인은 “Step away. Keep building.”이며, 라이트 버전도 전환해서 비교할 수 있다.

## 디자인 토큰

- Canvas `#f7f8fc`: 차가운 백색. Paper `#ffffff`: 콘텐츠 표면.
- Ink `#19213b`: 제목과 본문. Muted `#657087`: 보조 문장.
- Orbit `#535bd5`: 주요 동작. Line `#e0e4f0`: 경계. Soft `#eef0f8`: 보조 표면.
- 영문 페이지와 브랜드: Public Sans. 폰트와 OFL 라이선스는 로컬에 포함한다.
- 데스크톱 본문 폭 1160px, 두 줄의 큰 영문 제목, 넉넉한 섹션 간격. 모바일은 1열로 전환한다.
- 첫 화면의 시각적 포인트는 검정 우주 배경과 별빛·성운으로 표현한다. 초기 궤도선과 별 문양은 2026-09-22 피드백에 따라 교체했다. 실제 제품 이미지의 색과 내용을 수정하지 않는다.

## 페이지 구성

```text
로고   기능 / 워크플로 / FAQ                 GitHub / 다운로드

                Step away. Keep building.
               설명 / 다운로드 / 실제 데모

        [대화 화면] [터미널 화면]      실제 앱 이미지

           Claude Code와 Codex를 한 작업 공간에서

      계정 자동 전환    /    협업과 완료 검증    /    예약과 알림

           작업 배분 → 병렬 실행 → 검증과 수정

                      자주 묻는 질문
                  OS별 릴리스 확인 / 문서
```

## 정확성 원칙

- 지원 에이전트는 Claude Code와 Codex로 한정한다.
- 무제한 사용이나 성능 배수, 이용자 수, 고객 후기, 보장된 작업 완료를 주장하지 않는다.
- 계정 자동 전환은 등록된 사용 가능한 계정이 있을 때 이루어진다.
- 자동 수정에는 완료 조건과 재시도 한도가 있다.
- 데모 영상은 기존 제품 녹화다. 웹페이지의 기능 설명용 다이어그램은 실제 앱 UI로 표시하지 않는다.
- 다운로드 버튼은 최신 GitHub 릴리스에서 운영체제에 맞는 설치 파일로 연결한다. 파일을 확인할 수 없으면 릴리스 페이지로 안내한다.
- Astera는 Apache-2.0 오픈소스이며 Claude Code/Codex의 이용 요금은 별도다.

## 완료 기준

1. 실제 앱 이미지, 원본 로고, 데모 영상이 로컬에서 로드된다.
2. 대화/터미널 화면 전환, 데모 대화상자, FAQ, 메뉴, 명암 비교가 동작한다.
3. 1440px, 768px, 390px, 320px 폭에서 레이아웃을 검토한다.
4. 페이지 콘솔 및 네트워크 오류를 확인하고 키보드 포커스와 모션 감소 설정을 고려한다.
5. 실행 방법과 시안의 남은 결정을 기록한다.

## 검증 결과

- 1440 / 768 / 390 / 320 CSS px iframe에서 가로 넘침 0건, 이미지 누락 0건, 내부 앵커 누락 0건. Public Sans 두 굵기 모두 로드됨.
- `design/review.html`에서 실제 페이지의 상호작용 17개 통과. 미리보기 전환, 확대 창, 영상 메타데이터와 재생 진행, 닫기 시 재생 정리, FAQ, 모바일 메뉴, 테마 전환을 확인했다.
- 최초 검증에서 닫힘 이벤트가 지연되어 미디어 정리가 늦어지는 문제를 재현했다. 닫기/배경 클릭/취소 동작이 같은 정리 함수를 즉시 실행하도록 수정한 뒤 해당 검사가 통과했다.
- 기본 페이지를 다시 로드했을 때 콘솔 오류와 네트워크 오류는 없었다. 미디어 검사에서 로드 중인 영상을 곧바로 닫으면 취소된 미디어 요청이 `ERR_FAILED`로 기록됐다. 세 영상 모두 메타데이터가 로드됐으며 Jobs 영상은 재생 시간이 진행되는 것을 확인했다.
- `node --check main.js` 통과. 로컬 참조, 중복 ID, 단일 h1, 영문 카피 검사 통과. 이미지·영상 5개의 SHA-256이 앱 원본과 일치한다.
- `design/desktop.png`, `design/mobile-review.png`에 확인한 캡처를 보관했다. 마지막 추가 캡처는 Astera 브라우저가 `screenshot: the page did not paint within 5 s — the window may be minimised`를 반환하여 중단했다.
- 키보드 포커스 스타일과 reduced-motion CSS를 구현했다. 키보드만으로 전체 흐름을 탐색하는 검증 및 다른 브라우저/실기기 검증은 수행하지 않았다.

## 다음 검토

### 다크 시안 추가 — 2026-09-21

- 사용자 요청: “어두운 배경으로도 있으면 좋을 것 같다”. 기존 영문 구성에 짙은 남색 버전을 추가했다.
- 배경 `#0b1020`, 카드 `#131b30`, 본문 `#eef1ff`, 보조 글자 `#abb5cc`, 라벤더 액센트 `#b1b5ff`. 계정 경고, 아바타, 앱 프레임, 궤도도 다크 배경에 맞췄다.
- `?theme=dark` / `?theme=light`로 처음부터 원하는 시안을 연다. 상단·하단 전환 버튼을 동기화하고, URL에 선택을 반영해 새로고침 시 유지한다. 사용자 선택에 따라 쿼리가 없으면 다크가 기본값이며, `?theme=light`일 때 라이트로 시작한다. 비교 페이지도 같은 규칙을 따른다.
- 비교 페이지에도 Light/Dark 선택을 제공한다.
- 변경 전 기존 상호작용 17개 통과. 변경 후 상호작용 20개 통과, 양쪽 테마의 1440/768/390/320px에서 가로 넘침·누락 이미지·누락 앵커 0건. 상단/하단 버튼 상태 및 URL 동기화, 라이트/다크 전환 후 새로고침 확인. 기본 페이지 콘솔·네트워크 오류 없음. `node --check main.js` 통과.
- 다크 데스크톱·모바일·기능 영역을 실제 Astera 브라우저에서 확인하고 `design/dark-*.png`로 저장했다. 이전 시안의 캡처는 비교용으로 유지한다.

### 이후 결정

### 제품 미리보기 기본값 변경 — 2026-09-21

- 사용자 요청에 따라 첫 제품 화면을 Chat view에서 Terminal view로 변경했다.
- 초기 활성 탭, `aria-pressed`, 원본 이미지, 이미지 크기, 대체 텍스트를 Terminal view 기준으로 함께 맞췄다. Chat view 전환 동작과 원본 이미지는 그대로 유지한다.
- 변경 전 브라우저 검사는 “Terminal preview is the initial view”에서 실패했고, 변경 후 전체 상호작용 검사 21개가 통과했다. 기본 페이지 콘솔·네트워크 오류 없음.

### 기능·사용법 Guide 추가 — 2026-09-21

- 기존 다크 기본값, Public Sans, 색상 토큰, 실제 제품 이미지와 영상, 영어 중심 콘텐츠를 유지한다.
- `guide.html`에 개념 설명 → 첫 세션 4단계 → 기능별 사용법 → 선택 기능 → FAQ·단축키를 구성한다. 긴 본문은 데스크톱 고정 목차와 모바일 접이식 목차로 탐색한다.
- 실제 앱의 `README.md`, `docs/jobs.md`, `docs/slack-bot-setup.md`, `src/core/i18n/messages/en.ts`, `NewSessionDialog.tsx`의 기능과 UI 이름을 근거로 작성했다.
- Terminal/Chat은 생성 시 선택하며 실행 중 변경하지 않는다는 점, 계정별 사용량 제한, Jobs의 수동 병합과 예약 회차 차이, 실험적 기능의 선택 활성화를 설명한다.
- 메인 메뉴와 푸터에 Guide를 연결하고, 외부 기술 문서는 푸터와 가이드 안에서 접근한다. 페이지 이동 시 사용자가 선택한 테마를 URL로 전달한다.
- 별도 의존성 없이 기존 `main.js`의 미리보기·데모·메뉴·테마 동작을 공유한다. Guide만의 스타일과 목차 동작은 `guide.css`, `guide.js`에 둔다.
- 검증: Guide 상호작용 30개(데스크톱 다크·320px 라이트), 메인 페이지 22개 통과. 두 페이지 각각 다크·라이트의 1440/768/390/320px에서 가로 넘침·이미지 누락·앵커 누락 0건. 목차 펼침/접힘, 항목 이동, 현재 섹션 표시, 실제 데모 메타데이터·재생·닫기까지 확인했다.
- 실제 링크 클릭으로 메인 → Guide → 메인 Features 이동 시 라이트 테마와 목적지 앵커 유지, 새로고침 유지, 쿼리 없는 Guide의 다크 기본값을 확인했다. 일반 페이지 로드의 콘솔·네트워크 오류 없음. 영상 검사에서는 재생 중 닫기로 취소된 요청이 `ERR_FAILED`로 기록되지만 세 영상의 재생 가능한 메타데이터와 Jobs 영상의 재생 진행은 정상 확인했다.
- HTML 3개 파일의 로컬 참조·앵커·중복 ID 검사, 인라인 스크립트와 `main.js`/`guide.js` 문법 검사, `git diff --check` 통과. 새 의존성 없음.
- 브라우저 캡처는 `screenshot: the page did not paint within 5 s — the window may be minimised`로 실패하여 시각 캡처 검토는 미완료다. 같은 환경에서 부드러운 스크롤도 프레임을 기다려 멈췄고, 검사용 페이지에서만 애니메이션을 끄면 목적지 위치와 목차 표시가 정상임을 확인했다. 실제 페이지의 스크롤 설정은 유지한다. 저장된 캡처는 이전 소개 페이지 시안이며 Guide를 포함하지 않는다.

### 기능 가이드 보강과 누락 검토 — 2026-09-22

- 앱 `README.md`의 기능 목록을 기준으로 실제 화면 컴포넌트와 영어 UI 문구를 대조했다. `UnderstandingView`, `RecordDetail`, `FlowDiagram`, `HistoryBrowser`, `LocalHistoryDialog`, `FileExplorer`, `App`의 설정 화면이 주요 근거다.
- How It Works를 독립 목차로 올리고, 활성화 → 새 세션에서 작업 선언 → 완료 기록 열기 → 흐름과 근거 읽기를 설명했다. 상태, 검증 결과의 출처, 재생성의 범위, 기록이 없는 경우도 구분한다.
- 앱 README에는 How It Works GIF와 시작 순서, 상세 가이드 링크를 추가했다. `docs/how-it-works.md`와 `docs/usage-guide.md`를 문서 허용 목록에 등록했다. GIF는 앱의 실제 컴포넌트에 예시 데이터를 넣은 14초 설명용 시연임을 명시한다. Jobs는 앱 README의 기존 GIF를 유지하고 보는 포인트를 덧붙였다.

| 앱 기능 | 웹 Guide 위치 | 이번 검토 결과 |
| --- | --- | --- |
| 프로젝트·터미널·분할 창 | `#quickstart`, `#workspace` | 시작 순서와 프로젝트 터미널 안내 |
| Terminal·Chat·첨부 | `#sessions` | 생성 시 선택 유지, 파일·이미지 첨부 안내 보강 |
| 세션 히스토리 | `#sessions` | 미리보기와 Resume 구분, 이동·숨김 프로젝트 복구 추가 |
| 계정·자동 전환·설정 가져오기 | `#accounts` | 설정 가져오기와 새 세션 적용 범위 추가 |
| Smart Resume | `#optional` | 선택 활성화와 기본 resume 복귀 안내 유지 |
| 세션 예약·Slack | `#scheduling`, `#slack` | 실행 조건, 스레드 답장, 상세 설정 문서 연결 유지 |
| Jobs·완료 검증·예약 Run | `#jobs` | 수동 병합, 재시도 한도, 예약 회차 차이 안내 유지 |
| How It Works | `#how-it-works` | 독립 가이드, 기록·읽기·문제 확인 절차 추가 |
| 파일 편집·Markdown·삭제 복원 | `#workspace` | Markdown 모드, Local History 복원·보존 한계 추가 |
| Run | `#workspace` | 구성 선택, 감지된 구성 저장, 출력 확인 보강 |
| Worktree·GitHub PR | `#workspace` | 생성 조건, 커밋, 대상 브랜치와 PR 확인 보강 |
| Design Mode | `#workspace` | 노트 전달, 개수 제한, 탐색 시 해제 보강 |
| Agent browser | `#optional` | 새 세션과 로컬 앱 제한 안내 유지 |
| 테마·언어·단축키 | `#preferences`, `#faq` | 설정 위치를 독립 목차에 정리 |
| 데스크톱 알림·업데이트 | `#preferences` | 알림 조건, 수동 업데이트와 플랫폼 차이 추가 |

- 검증: 앱 문서 3개의 로컬 참조 36개 정상, How It Works/Jobs GIF의 모든 프레임 디코딩과 반복 설정 정상. 웹 HTML 3개 및 JavaScript 문법 검사 정상. 웹 Guide의 두 테마 × 4개 폭에서 가로 넘침·이미지 누락·앵커 누락 0건, 일반 로드 콘솔·네트워크 오류 없음.
- 현재 브라우저 실행에서는 데스크톱 28/30, 320px 전환 후 27/30 상호작용 검사가 통과했다. 실패 항목은 영상 재생 진행, 현재 섹션 강조, 폭 전환 후 목차 상태다. 문서가 `hidden`이고 검사 중 애니메이션 프레임을 받지 못한 것을 확인했으며, Chromium은 백그라운드 절전을 이유로 영상 재생을 중단했다고 응답했다. 표시된 창에서의 동적 재검증은 남아 있다. 실패를 숨기지 않고 검토 페이지에 렌더링 상태를 함께 출력한다.

### 한국어·영어 전환 — 2026-09-22

- 첫 방문은 브라우저의 기본 언어를 따른다. 한국어 환경은 한국어, 나머지는 영어로 표시한다. 명시적 `lang=ko/en` URL → 직접 선택한 저장 값 → 브라우저 언어 순서로 결정한다. 지원하지 않는 값은 다음 선택 기준으로 넘긴다.
- 메인·Guide의 상단과 하단에서 언어를 전환한다. 테마·현재 URL 앵커·읽던 섹션의 위치를 유지하고 페이지 이동 링크에도 언어와 테마를 전달한다. 저장소가 차단되어도 전환과 URL 보존은 동작한다.
- 영어 HTML을 원문으로 두고 `locales/ko.js`의 번역을 `data-i18n` 속성으로 연결했다. `language.js`가 초기 선택·전환을 담당하고, `main.js`의 동적 메뉴·테마·미디어 설명도 번역한다. 기존 버튼 요소와 이벤트 처리를 유지하며 새로운 의존성은 없다.
- 메뉴·본문·FAQ·설명 다이어그램·페이지 제목과 메타 설명·접근성 문구까지 번역했다. 앱 설정과 How It Works 상태 이름은 실제 한국어 메시지 파일을 확인했다. 한국어 설치·문서 링크는 기존 앱의 `README.ko.md`로 연결한다. 원본 제품 이미지와 녹화의 내용은 그대로다.
- 사용자의 후속 요청에 따라 메인·Guide의 큰 제목과 주요 기능 카드 제목 21개는 두 언어 모두 영어로 유지한다. 해당 제목에 `lang="en"`을 지정하고 사용하지 않는 한국어 번역 항목은 제거했다. 작은 사용법 제목·본문·버튼은 선택한 언어로 표시한다.
- 최상단 메뉴도 영어로만 표시한다. 메인·Guide의 데스크톱·모바일 메뉴와 상단 Download 버튼은 영어로 고정하고, 언어 전환 버튼은 전환할 언어를 계속 표시한다. 상단 메뉴의 한국어 번역 연결과 전용 번역 항목 18개를 제거했다.
- 사용자의 후속 요청대로 How It Works 및 선택 기능 섹션의 Experimental 배지를 제거했다. Smart Resume·How It Works·Agent browser의 기본 비활성화·새 세션 적용 안내는 유지한다.
- 한국어 글꼴 대체 목록과 단어 단위 줄바꿈을 적용했다. 390px에서 언어 버튼이 추가된 헤더가 넘치는 것을 재현한 뒤 작은 화면의 간격·아이콘 크기를 조정했다.
- 검증: 언어 기능 38개 통과(자동 선택, URL/저장 값 우선순위, 잘못된 값, 저장소 차단, 전환·새로고침·읽던 위치, 전체 번역 연결, 큰 제목의 영어 유지). 메인 22개·Guide 30개 기존 상호작용이 양쪽 언어에서 모두 통과했다. 브라우저가 visible 상태에서 렌더링 프레임을 받은 것을 확인했으며, 이전 단계에서 남았던 영상 재생과 목차 강조 검사도 통과했다.
- 두 페이지 × 두 언어 × 두 테마 × 1440/768/390/320px의 총 32가지 조합에서 넘침·이미지 누락·앵커 누락 0건. 일반 페이지 로드 콘솔·네트워크 오류 없음. 정적 참조·스크립트 문법·번역 키 및 `git diff --check`를 검토했다.
- 큰 제목을 영어로 유지한 한국어 메인·Guide의 최종 캡처를 확인하고 `design/ko-desktop.png`, `design/ko-guide.png`에 보관했다. 중간에 추가 캡처가 브라우저의 열기 시간 초과로 중단됐지만, 최종 화면은 정상적으로 캡처하고 확인했다.

### 기능 GIF 추가 — 2026-09-22

- 앱 README에 포함된 실제 녹화 `rolling-demo.gif`, `schedule-demo.gif`, `jobs-demo.gif`를 원본 그대로 복사했다. 메인의 해당 기능 카드와 Guide의 계정·예약·Jobs 설명에 삽입했다. 기존 개념도와 실제 녹화는 캡션으로 구분한다.
- GIF를 선택하면 기존 전체 데모 영상을 연다. 설명과 버튼의 접근성 이름은 한국어·영어로 제공하며, 상단 메뉴와 큰 제목은 영어로 유지한다.
- GIF는 지연 로드하고 원본 크기를 지정해 로드 전에도 공간을 확보한다. 모션 감소 설정에서는 각 GIF의 첫 프레임 PNG를 표시한다. 새 의존성은 없다.
- GIF 추가 후 언어 검사 38개, 두 페이지·두 언어·두 테마·네 가지 폭의 레이아웃 검사 32개 통과. 검사 페이지에서는 지연 로드 이미지를 불러오고 디코딩한 뒤 누락 여부를 검사한다.
- 세 GIF의 SHA-256이 앱 원본과 일치하며 총 302프레임을 모두 디코딩했다. 무한 반복 설정, 첫 프레임 PNG의 픽셀 일치, 각 페이지의 GIF 세 개와 모션 감소용 연결, 번역 키·정적 참조·JavaScript 문법 검사를 통과했다.
- 추가 브라우저 확인에서는 메인의 양쪽 언어에서 상단 영문 메뉴와 세 GIF의 영상 열기·닫기 동작을 확인했다. 이후 Guide 로드가 `ERR_CONNECTION_FAILED`로 중단되어 Guide의 새 GIF 버튼 및 최종 시각 캡처 확인은 남아 있다. 저장된 한국어 캡처는 상단 메뉴 영어 고정과 GIF 추가 전의 화면이다.

### 검정 우주 히어로 — 2026-09-22

- 지정한 `section.hero`에 검정 하늘 `#020306`과 옅은 청보라 성운을 적용했다. 하단은 기존 페이지 배경으로 자연스럽게 이어진다. 큰 별 문자와 궤도선 대신 불규칙한 작은 빛점 210개와 밝은 별의 부드러운 빛 번짐을 사용한다. 영어 제목, 번역된 본문, 제품 화면은 같은 위치에 둔다.
- 마우스 위치에 따라 가까운 별·먼 별·성운이 서로 다른 깊이로 움직인다. 배경은 클릭을 가로채지 않으며 제목과 버튼은 고정된다. 별의 진입 효과는 최대 4.8초로 끝난다. 터치 기기와 모션 감소 설정에서는 배경이 정지한다.
- SVG·CSS와 메인에만 로드하는 `hero.js`로 구현했다. 마우스 이벤트를 프레임마다 한 번으로 모으고 상시 렌더링 루프는 사용하지 않는다. 영역 이탈, 입력 취소, 모션 설정 변경, 탭 숨김 시 위치와 대기 프레임을 초기화한다. 교체한 히어로 장식의 HTML·CSS 참조도 함께 정리했다.
- 검증: 언어 검사 38개, 레이아웃 32개 조합 통과. 메인 26개·Guide 30개 상호작용 검사가 영어·한국어에서 모두 통과했다. 초기 마우스 검사 한 번이 100ms 대기 안에 갱신을 관찰하지 못해, 검사에서 다음 렌더링 프레임을 기다리도록 변경했다. 이후 전체 재검사 통과. 정적 문법·참조·번역 키 검사 통과.
- 별도 이벤트 검사에서 프레임 묶기, 이동량 제한, 영역 이탈, 실시간 모션 감소 변경, 거친 포인터·터치 입력, 탭 숨김 처리를 확인했다. 데스크톱 다크, 390px 다크, 데스크톱 라이트 캡처를 직접 확인했다. 일반 페이지 콘솔·네트워크 오류 없음. 캡처는 `design/space-desktop.png`, `design/space-mobile-review.png`, `design/space-light-review.png`에 보관했다.
- 앞 단계에서 연결 오류로 남았던 Guide의 데모 재생·닫기 검사도 이번 브라우저 실행에서 통과했다.

사용자에게 전체 인상, 헤드라인, 기능 설명의 밀도를 피드백 받는다. 운영 프레임워크, 도메인, 배포 방식, 추가 언어는 이후 구현 단계에서 결정한다.

### 공개 배포와 검색 노출 — 2026-09-23

- 영문 메인·Guide와 크롤러가 본문을 바로 읽을 수 있는 한국어 정적 페이지를 GitHub Pages의 `astera.run`에 배포했다. 한국어 페이지는 기존 번역 카탈로그에서 생성한다.
- 네 페이지에 대표 URL·언어별 대체 URL·공유 메타정보를 연결하고, 메인에는 실제 앱 정보를 담은 구조화 데이터를 넣었다. 사이트맵과 전체 크롤러 허용 `robots.txt`를 공개했다.
- 검색 관련 7개 검사와 운영체제별 다운로드 검사 8개가 통과했다. 로컬 브라우저의 한국어 홈→Guide→영어 전환에서 콘솔·네트워크 오류가 없었고, 공개 URL 4개와 사이트맵·robots가 HTTP 200을 반환했다.
